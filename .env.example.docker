# Dùng với frontend/docker-compose (build args). Copy thành .env cùng thư mục nếu cần override.
#
# API mà trình duyệt trên máy host gọi tới (backend compose mặc định publish port 3000).
NEXT_PUBLIC_API_URL=http://localhost:3000
